# os模块需要导入才能使用
# os库主要是用来高效率的管理文件的一个库（模块），里面有相应的函数，供我们使用
import os
import os.path
# 绝对路径和相对路径的概念
# 绝对路径就是指文件在硬盘上真是存在的路径
# 例如我的(C文件)数电作业存放在d:\作业
# 我的（D文件）数电实验课设.ppt存放在d:\作业:\数电实验
# 那么D文件相对于c文件的相对路径就是\数电实验
# 如果要返回（../是返回上一级目录）
