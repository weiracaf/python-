# 从012301230匹配r'0.*0'
import re
str1 = '012301230'
a = re.search(r'0.*0', str1)
# 这样的话就是匹配0.....0，就是两边有两个0就ok，那么结构应该是01230吧
print(a.group())
# 结果不是01230，因为re库是默认贪婪匹配的
# 就是匹配最长的字符串
a = re.search(r'0.*?0', str1)  # 加上应该？就是非贪婪匹配喽
print(a.group())
